export class Ingredient {
    // Use this typescript shortcut to create properties for the model and pass values to those props in the constructor 
    constructor(public name: string, public amount: number){}
}